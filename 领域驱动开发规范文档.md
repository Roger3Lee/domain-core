# 领域驱动开发规范（DDD Development Rules）

> **用途**: 本文档为 Claude Code 提供领域驱动开发的完整规范和流程指导
> **配合技能**: `ddd-code-generator` - 自动生成 DDD 领域模型代码
> **适用场景**: 从领域模型配置到代码生成的完整开发流程

---

## 📋 目录
- [1. 框架核心功能](#1-框架核心功能)
- [2. 完整开发流程](#2-完整开发流程)
- [3. 重要注意事项与约束](#3-重要注意事项与约束)
- [4. 领域模型配置规范](#4-领域模型配置规范)
- [5. 领域模型使用指南](#5-领域模型使用指南)
- [6. 分层架构规范](#6-分层架构规范)
- [7. 框架适用场景与限制](#7-框架适用场景与限制)
- [8. 最佳实践](#8-最佳实践)

---

## 1. 框架核心功能

> 💡 **本章重点**: domain-core 框架提供的核心能力，了解框架能做什么。

### 1.1 聚合根模式（Aggregate Root）

**功能**: 聚合根是领域模型的入口，统一管理关联实体的生命周期。

```java
// 聚合根继承 BaseAggregateDomain
public class FamilyDomain extends BaseAggregateDomain<FamilyDomain, FamilyService> {
    // 聚合根字段
    private Long id;
    private String name;
    
    // 一对一关联实体
    private FamilyAddressDomain familyAddress;
    
    // 一对多关联实体
    private List<FamilyMemberDomain> familyMemberList;
}
```

**关键特性**:
- ✅ 自动管理关联实体的外键关系
- ✅ 支持级联插入、更新、删除
- ✅ 内置 `_service` 引用，支持链式加载关联数据
- ✅ 自动维护冗余字段（如 `family_name` 冗余到关联表）

### 1.2 智能加载机制

**功能**: 按需加载关联数据，避免不必要的数据查询。

```java
// 基础加载
FamilyDomain family = FamilyDomain.load(1L, familyService);

// 加载关联数据（支持链式调用）
family.loadRelated(FamilyDomain.FamilyAddressDomain.class)
      .loadRelated(FamilyDomain.FamilyMemberDomain.class);

// 带条件加载
family.loadRelated(FamilyDomain.FamilyMemberDomain.class, query -> {
    query.eq(FamilyMemberDomain::getType, "父亲")
         .orderBy(FamilyMemberDomain::getCreateTime, Order.DESC);
});
```

**优势**:
- 🚀 懒加载策略，按需加载
- 🔍 支持复杂的 Lambda 查询条件
- ⛓️ 链式调用，代码简洁

### 1.3 增量更新（Delta Update）

**功能**: 自动对比新旧对象，只更新发生变化的字段。

```java
// 1. 加载原始数据
FamilyDomain oldFamily = FamilyDomain.load(1L, familyService);

// 2. 创建新对象（修改了部分字段）
FamilyDomain newFamily = new FamilyDomain();
newFamily.setId(1L);
newFamily.setName("新名称");  // 只修改了名称

// 3. 执行增量更新（只更新 name 字段）
familyService.update(newFamily, oldFamily);
```

**优势**:
- 📊 自动字段对比，减少不必要的数据库更新
- 🔒 降低并发冲突风险
- ⚡ 提升更新性能

### 1.4 类型安全查询（Lambda Query）

**功能**: 使用 Lambda 表达式构建类型安全的查询条件。

```java
// 构建查询条件（编译期类型检查）
LambdaQuery<FamilyDomain> query = LambdaQuery.of(FamilyDomain.class);
query.eq(FamilyDomain::getName, "张氏家族")
     .gt(FamilyDomain::getPersonCount, 2)
     .or()
     .like(FamilyDomain::getName, "李氏")
     .orderBy(FamilyDomain::getCreateTime, Order.DESC);

// 查询操作
FamilyDomain one = familyService.queryOne(FamilyDomain.class, query);
List<FamilyDomain> list = familyService.queryList(FamilyDomain.class, query);
IPage<FamilyDomain> page = familyService.queryPage(FamilyDomain.class, pageDomain, query);

// 统计数据总量
Long count = familyService.queryCount(FamilyDomain.class, query);
```

**支持的查询操作符**:
- 比较: `eq`, `ne`, `gt`, `ge`, `lt`, `le`
- 模糊: `like`, `likeLeft`, `likeRight`, `notLike`
- 范围: `in`, `notIn`, `between`
- 空值: `isNull`, `notNull`
- 逻辑: `and()`, `or()`, 嵌套查询
- **统计**: `queryCount()` - 统计符合条件的数据总量

### 1.5 级联操作（Cascade Operations）

**功能**: 聚合根自动处理关联实体的插入、更新、删除。

```java
// 插入（自动处理外键和冗余字段）
FamilyDomain family = new FamilyDomain();
family.setName("张氏家族");
family.setFamilyAddress(address);
family.setFamilyMemberList(members);
Integer id = familyService.insert(family);  // 所有实体 ID 自动回填

// 删除（级联删除关联实体）
familyService.delete(1L);  // 自动删除 family_address 和 family_member
```

**级联策略**:
- 📥 插入: 先插入聚合根，获取 ID 后插入关联实体
- 📝 更新: 一对一执行 UPDATE，一对多执行 MERGE（删除旧的 + 插入新的）
- 🗑️ 删除: 先删除关联实体，再删除聚合根

### 1.6 多数据库支持

**支持的数据库**:
- ✅ MySQL
- ✅ PostgreSQL
- ✅ Oracle
- ✅ PolarDB

**数据库特定的批量优化**:
- MySQL: `ON DUPLICATE KEY UPDATE`, `REPLACE INTO`
- PostgreSQL: `ON CONFLICT`, `MERGE`
- Oracle: `INSERT ALL`, `MERGE INTO`

### 1.7 智能外键与冗余字段管理

**功能**: 自动处理外键关联和冗余字段同步。

```xml
<!-- 外键映射 -->
fk="id:family_id"

<!-- 多字段外键 -->
fk="id:family_id|type:member_type"

<!-- 冗余字段映射 -->
redundancy="name:family_name"

<!-- 常量值填充 -->
fk="'ACTIVE'(String):status(VARCHAR)"
redundancy="'张氏家族'(String):family_name(VARCHAR)"
```

**特性**:
- ✅ 自动填充外键字段
- ✅ 自动同步冗余字段
- ✅ 支持多字段联合外键
- ✅ 支持常量值作为字段源

### 1.8 LoadFlag 查询控制

**功能**: 精确控制关联数据的加载行为和查询参数。

```java
// 控制加载哪些关联实体
FamilyDomain.LoadFlag loadFlag = new FamilyDomain.LoadFlag() {{
    setLoadFamilyMemberDomain(true);
    setLoadFamilyAddressDomain(false);
    
    // 配置查询条件
    BaseLoadFlag.Query memberQuery = new BaseLoadFlag.Query();
    memberQuery.setFilter(query.getFilter());
    memberQuery.setOrder(Arrays.asList(
        new LambdaOrderItem(FamilyMemberDomain::getName, Order.ASC)
    ));
    memberQuery.setSelect(Arrays.asList("id", "name", "type"));
    getQuery().put("FamilyMemberDomain", memberQuery);
}};

FamilyFindDomain findDomain = FamilyFindDomain.builder()
    .key(1L)
    .loadFlag(loadFlag)
    .build();

FamilyDomain family = familyService.find(findDomain);
```

---

## 2. 完整开发流程

> ⚠️ **本章重点**: 从配置到部署的完整开发流程，按步骤执行。

### 2.1 端到端开发流程

```
第1步: 编写领域模型配置 (XML)
    ↓
   编写 domain-{name}.xml，定义聚合根和关联关系
    
第2步: 准备 SQL DDL 文件
    ↓
   编写表结构定义，包括聚合根表和关联表
    
第3步: 代码生成 (ddd-code-generator skill)
    ↓
   使用 ddd-code-generator 技能自动生成完整领域层代码
   生成内容: Domain, Repository, Service, Mapper, Controller 等
    
第4步: 业务逻辑开发
    ↓
   在生成的领域模型基础上，编写应用层业务逻辑
   位置: AppService 层，处理业务编排和事务管理
    
第5步: 接口测试与部署
    ↓
   通过生成的 Controller 进行 API 测试
   部署到目标环境
```

### 2.2 关键原则

1. **配置驱动**: 所有领域模型结构通过 XML 配置定义，不手动编写领域层代码
2. **自动生成**: 使用 `ddd-code-generator` 技能生成完整代码
3. **禁止修改**: 生成的代码（Domain/Service/Repository/Mapper）**严禁手动修改**
4. **扩展而非修改**: 在 AppService 层扩展业务逻辑，不修改生成的 DomainService

### 2.3 代码生成工作流

```
┌─────────────────────────────────────────────────────────┐
│  第 1 步: 准备 SQL DDL 文件                              │
└────────────────────┬────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────┐
│  第 2 步: 编写 domain-{name}.xml                        │
└────────────────────┬────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────┐
│  第 3 步: 调用 ddd-code-generator 技能                   │
│  🛠️ Skill: ddd-code-generator                          │
│  输入: SQL DDL + domain-{name}.xml                      │
│  输出: 完整的领域层代码                                  │
└────────────────────┬────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────┐
│  第 4 步: 验证生成的代码                                 │
│  ✅ 检查生成的 Domain/Service/Repository 等类           │
│  ✅ 确认编译通过                                        │
└────────────────────┬────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────┐
│  第 5 步: 在 AppService 层编写业务逻辑                   │
│  ✅ 不修改生成的代码                                    │
│  ✅ 在应用层扩展业务功能                                │
└─────────────────────────────────────────────────────────┘
```

### 2.4 使用 ddd-code-generator 技能

#### 调用方式

```
使用技能: ddd-code-generator

输入参数:
- domain_xml: domain-{name}.xml 文件路径
- sql_file: SQL DDL 文件路径（MySQL 或 PostgreSQL）
- output_dir: 代码输出目录
- base_package: Java 基础包名（如 com.example.project）
- dialect: 数据库类型（mysql 或 postgresql）
```

#### 示例调用

```
请帮我生成家庭领域模型的代码：
- domain XML: domain-family.xml
- SQL DDL: family-mysql.sql
- 输出目录: src/main/java
- 基础包名: com.artframework.sample
- 数据库类型: mysql
```

### 2.5 生成的代码结构

```
{base_package}/
├── domains/                                    # 领域层
│   └── family/                                 # 家庭领域
│       ├── domain/
│       │   ├── FamilyDomain.java              # 聚合根（包含内部类：关联实体）
│       │   └── FamilyFindDomain.java          # 查询请求 DTO
│       │
│       ├── repository/                         # 仓储接口
│       │   ├── FamilyRepository.java          # 聚合根 Repository
│       │   └── impl/
│       │       ├── FamilyRepositoryImpl.java  # Repository 实现
│       │       └── FamilyMemberRepositoryImpl.java
│       │
│       ├── service/                            # 领域服务接口
│       │   ├── FamilyService.java
│       │   └── impl/
│       │       └── FamilyServiceImpl.java     # 领域服务实现
│       │
│       ├── convertor/                          # 对象转换器（MapStruct）
│       │   ├── FamilyConvertor.java
│       │   └── FamilyConvertorDecorator.java
│       │
│       └── lambdaexp/                          # Lambda 表达式
│           └── FamilyLambdaExp.java
│
├── entities/                                   # 数据库实体（DO）
│   ├── FamilyDO.java
│   ├── FamilyAddressDO.java
│   └── FamilyMemberDO.java
│
├── mappers/                                    # MyBatis Mapper
│   ├── FamilyMapper.java
│   ├── FamilyAddressMapper.java
│   └── FamilyMemberMapper.java
│
└── controllers/                                # REST 控制器
    └── FamilyController.java
```

---

## 3. 重要注意事项与约束

> 🚨 **本章极其重要**: 必须严格遵守的规则和注意事项。

### 3.1 代码生成约束

#### 🚫 绝对禁止的操作

```
❌ 禁止手动修改以下生成的文件：
   - *Domain.java（聚合根和关联实体）
   - *Service.java 和 *ServiceImpl.java（领域服务）
   - *Repository.java 和 *RepositoryImpl.java（仓储）
   - *Mapper.java（MyBatis Mapper）
   - *Convertor.java（对象转换器）
   - *LambdaExp.java（Lambda 表达式）

❌ 禁止在生成的代码中添加业务逻辑
❌ 禁止在生成的代码中添加新方法
❌ 禁止修改生成代码的注释
```

#### ✅ 正确的扩展方式

```
✅ 需要修改领域结构时：
   1. 修改 domain-{name}.xml 配置文件
   2. 重新运行 ddd-code-generator 技能
   3. 生成器会自动覆盖已存在的文件

✅ 需要扩展业务逻辑时：
   1. 在 AppService 层编写新的业务方法
   2. 调用生成的 DomainService 提供的基础 CRUD 方法
   3. 不要修改生成的 DomainService 代码

✅ 需要添加新的领域时：
   1. 创建新的 domain-{name}.xml 配置文件
   2. 在 SQL DDL 中添加新的表定义
   3. 运行 ddd-code-generator 技能生成代码
```

### 3.2 分层架构约束

```
❌ 禁止跨层调用：
   - Controller 不能直接调用 DomainService/Repository/Mapper
   - AppService 不能直接调用 Repository/Mapper
   - DomainService 不能包含业务编排逻辑

✅ 正确的调用链：
   Controller → AppService → DomainService → Repository → Mapper → Database
```

### 3.3 查询性能注意事项

```
⚠️ 避免 N+1 查询问题：
   ❌ 错误：循环中查询数据库
   ✅ 正确：批量查询后一次性加载关联数据

⚠️ 分页查询限制：
   - 框架默认限制最大 10000 条记录
   - 超过限制会被自动调整

⚠️ 复杂查询场景：
   - JOIN 查询 → 使用 MyBatis 自定义 Mapper
   - 统计查询（GROUP BY） → 使用 MyBatis 自定义 Mapper
   - 子查询 → 使用 MyBatis 自定义 Mapper
   - 跨聚合根操作 → 使用 MyBatis 自定义 Mapper
```

### 3.4 事务管理注意事项

```
✅ 事务应在 AppService 层管理：
   @Transactional(rollbackFor = Exception.class)
   public void createFamily(FamilyDTO dto) {
       // 业务逻辑
   }

⚠️ 自定义 SQL 操作也要纳入事务管理
⚠️ 跨多个 DomainService 的操作需要统一事务控制
```

### 3.5 领域模型使用注意事项

```
⚠️ 领域对象的生命周期：
   1. 加载 → FamilyDomain.load(id, service)
   2. 修改 → 设置字段值
   3. 保存 → service.update(newDomain, oldDomain)
   4. 删除 → service.delete(id)

⚠️ 序列化控制：
   - _service、_thisDomain 等内部字段不会序列化
   - 可以直接将领域对象返回给前端

⚠️ 类型安全查询：
   - 优先使用 Lambda 表达式
   - 避免使用字符串字段名
```

---

## 4. 领域模型配置规范

> ⚠️ **规则**: 领域模型通过 XML 配置定义，这是**唯一**需要手动编写的领域层配置文件。


### 4.1 domain-{name}.xml 配置结构

```xml
<domains>
    <!-- 定义一个聚合根领域 -->
    <domain name="family"                    <!-- 领域名称 → 生成 FamilyDomain -->
            description="家庭领域模型"         <!-- 领域描述 → Swagger API 文档 -->
            main-table="family">              <!-- 聚合根对应的数据库表 -->
        
        <!-- 一对多关联：家庭成员 -->
        <related description="家庭成员"
                 table="family_member"
                 many="true"
                 fk="id:family_id"
                 redundancy="name:family_name">
            
            <!-- 引用关系：成员绑定的家庭住址（一对一） -->
            <ref name="address" 
                 description="成员住址绑定"
                 table="family_address" 
                 many="false" 
                 fk="id:family_member_id"/>
        </related>
        
        <!-- 一对多关联：家庭住址 -->
        <related description="家庭住址" 
                 table="family_address" 
                 many="true" 
                 fk="id:family_id" 
                 redundancy="name:family_name"/>
    </domain>
    
    <!-- 可以定义多个聚合根领域 -->
    <!-- <domain name="order" ...> -->
</domains>
```

### 4.2 配置属性详解

#### 4.2.1 domain 元素（聚合根定义）

| 属性 | 必填 | 说明 | 格式要求 | 示例 |
|------|------|------|---------|------|
| `name` | ✅ | 领域名称 | 小写字母，用于生成类名 | `family` → `FamilyDomain` |
| `description` | ✅ | 领域描述 | 中文描述，用于 Swagger API 文档 | `家庭领域模型` |
| `main-table` | ✅ | 主表名 | 必须与 SQL DDL 中的表名一致 | `family` |

#### 4.2.2 related 元素（关联关系定义）

**用途**: 定义聚合根与关联实体之间的关系，关联实体受聚合根管理。

| 属性 | 必填 | 说明 | 格式要求 | 示例 |
|------|------|------|---------|------|
| `description` | ✅ | 关联实体描述 | 中文描述 | `家庭成员` |
| `table` | ✅ | 关联表名 | 必须与 SQL DDL 中的表名一致 | `family_member` |
| `many` | ✅ | 关联类型 | `true`=一对多（生成List）, `false`=一对一（生成单对象） | `true` |
| `fk` | ✅ | 外键映射 | 格式：`主表字段:关联表字段`，多字段用 `\|` 分隔 | `id:family_id` |
| `redundancy` | ❌ | 冗余字段映射 | 格式：`主表字段:关联表冗余字段`，多字段用 `\|` 分隔 | `name:family_name` |
| `deletable` | ❌ | 是否可删除 | `true`/`false`，默认 `true` | `true` |
| `implement` | ❌ | 实现接口 | 自定义接口全限定名 | `com.example.IFamilyMember` |

**外键映射（fk）格式详解**:

```xml
<!-- 单字段外键 -->
fk="id:family_id"

<!-- 多字段外键（使用 | 分隔） -->
fk="id:family_id|type:member_type"

<!-- 带类型转换的外键 -->
fk="id(Long):family_id(BIGINT)"

<!-- 使用常量值作为外键源 -->
fk="'ACTIVE'(String):status(VARCHAR)"
fk="1(Integer):status(INT)"
```

**冗余字段（redundancy）格式详解**:

```xml
<!-- 单字段冗余 -->
redundancy="name:family_name"

<!-- 多字段冗余（使用 | 分隔） -->
redundancy="name:family_name|type:family_type"

<!-- 带类型转换的冗余 -->
redundancy="name(String):family_name(VARCHAR)"

<!-- 使用常量值填充冗余字段 -->
redundancy="'张氏家族'(String):family_name(VARCHAR)"
redundancy="100(Integer):person_count(INT)"
```

**特性**:
- ✅ 受聚合根生命周期管理
- ✅ 自动级联插入、更新、删除
- ✅ 支持冗余字段自动同步
- ✅ 支持多字段外键和冗余
- ✅ 支持常量值作为外键或冗余字段源

#### 4.2.3 ref 元素（引用关系定义）

**用途**: 在关联实体内部引用其他实体数据，引用的实体不受聚合根管理。

**使用场景**:
- 引用其他业务实体的数据（如：成员绑定住址）
- 不需要级联操作（不随聚合根一起插入/删除）
- 仅用于数据查询和展示

| 属性 | 必填 | 说明 | 格式要求 | 示例 |
|------|------|------|---------|------|
| `name` | ✅ | 引用字段名称 | 小驼峰命名，用于生成字段名 | `address` → `getRefAddress()` |
| `table` | ✅ | 引用表名 | 必须与 SQL DDL 中的表名一致 | `family_address` |
| `many` | ✅ | 是否一对多 | `true`=生成List, `false`=生成单对象 | `false` |
| `fk` | ✅ | 外键映射 | 格式同 related，支持 `\|` 和常量 | `id:family_member_id` |
| `redundancy` | ❌ | 冗余字段映射 | 格式同 related | `name:address_name` |
| `description` | ❌ | 引用关系描述 | 中文描述 | `成员住址绑定` |

**特性**:
- ⚠️ 只能在 `<related>` 元素内部使用
- ❌ 不随聚合根级联插入、更新、删除
- ✅ 从父聚合根的 related 列表中懒加载（不需要单独查询）
- ✅ 使用 `CacheDomain` 包装，支持缓存
- ✅ 适用于查询展示，不适用于修改操作

#### 4.2.4 related 与 ref 的区别

| 特性 | `<related>` 关联 | `<ref>` 引用 |
|------|-----------------|-------------|
| **生命周期** | 受聚合根管理 | 独立生命周期 |
| **级联插入** | ✅ 自动插入 | ❌ 不自动插入 |
| **级联删除** | ✅ 自动删除 | ❌ 不自动删除 |
| **级联更新** | ✅ 自动更新 | ❌ 不自动更新 |
| **数据加载** | ✅ 独立查询 | ✅ 从父聚合根 related 列表过滤 |
| **存储方式** | 直接字段存储 | `CacheDomain` 包装 |
| **使用场景** | 聚合内部实体 | 引用外部实体 |

#### 4.2.5 ref 使用示例

生成的代码中，ref 使用 `CacheDomain` 实现懒加载：

```java
// 在 FamilyDomain.java 中生成的 FamilyMemberDomain 内部类
@NoArgsConstructor
@AllArgsConstructor
public static class FamilyMemberDomain extends BaseDomain {
    /**
    * 自增主键
    */
    @Getter
    @Setter
    @Schema(description = "自增主键")
    private Long id;
    
    /**
    * 家庭ID
    */
    @Getter
    @Setter
    @Schema(description = "家庭ID")
    private Long familyId;
    
    /**
    * 冗余字段：家庭名称
    */
    @Getter
    @Setter
    @Schema(description = "冗余字段：家庭名称")
    private String familyName;
    
    /**
    * 姓名
    */
    @Getter
    @Setter
    @Schema(description = "姓名")
    private String name;
    
    /**
    * 电话
    */
    @Getter
    @Setter
    @Schema(description = "电话")
    private String phone;
    
    /**
    * 成员关系
    */
    @Getter
    @Setter
    @Schema(description = "成员关系")
    private String type;

    // ===== ref 引用关系字段 =====
    @Schema(description = "Related to family_address")
    @JsonIgnore
    private CacheDomain<FamilyAddressDomain> refAddress;
    
    /**
    * Related to family_address - 懒加载 getter
    */
    public synchronized FamilyAddressDomain getRefAddress() {
        if(null == this.refAddress){
            FamilyDomain domain = (FamilyDomain)this.get_thisDomain();
            if(null == domain){
                return null;
            }

            // 从父聚合根的 familyAddressList 中过滤匹配的数据
            Predicate<FamilyAddressDomain> condition = x -> true;
            condition = condition.and(x -> 
                ObjectUtil.equals(
                    FamilyLambdaExp.familyMemberRefAddress_idSourceLambda.apply(this), 
                    FamilyLambdaExp.familyMemberRefAddress_familyMemberIdTargetLambda.apply(x)
                )
            );
            
            if (CollUtil.isNotEmpty(domain.getFamilyAddressList())) {
                this.refAddress = new CacheDomain<>(
                    domain.getFamilyAddressList().stream()
                        .filter(condition)
                        .findFirst()
                        .orElse(null)
                );
            } else {
                this.refAddress = new CacheDomain<>(null);
            }
        }
        return this.refAddress.getValue();
    }
    
    /**
    * REF setter
    */
    public synchronized void setRefAddress(FamilyAddressDomain ref) {
        if (ObjectUtil.isNotEmpty(ref)) {
            ListUtil.toList(ref).forEach(x -> x.set_thisDomain(this.get_thisDomain()));
            this.refAddress = new CacheDomain<>(ref);
        } else {
            this.refAddress = new CacheDomain<>(null);
        }
    }
    
    /**
    * 保存后处理 ref 数据
    */
    @Override
    public void afterSave(SaveState saveState) {
        FamilyDomain domain = (FamilyDomain)this.get_thisDomain();
        // 处理 refAddress
        if(ObjectUtil.isNotEmpty(this.refAddress) && 
           ObjectUtil.isNotEmpty(this.refAddress.getValue())){
            java.util.List<FamilyAddressDomain> list = new java.util.ArrayList<>();
            
            // 设置关联字段值
            for (FamilyAddressDomain refDomain : 
                 ListUtil.toList(this.refAddress.getValue())) {
                FamilyLambdaExp
                    .familyMemberRefAddress_familyMemberIdTargetSetLambda
                    .accept(refDomain, 
                        (Long)FamilyLambdaExp
                            .familyMemberRefAddress_idSourceLambda
                            .apply(this));
                list.add(refDomain);
            }

            // 将关联信息插入到主 domain 中
            if (CollectionUtil.isNotEmpty(domain.getFamilyAddressList())) {
                list.forEach(x -> {
                    // 引用类型，如果已经存在则不重复加入
                    if (!domain.getFamilyAddressList().contains(x)) {
                        domain.getFamilyAddressList().add(x);
                    }
                });
            } else {
                domain.setFamilyAddressList(list);
            }
        }
    }
}
```

// 使用示例
FamilyDomain family = FamilyDomain.load(1L, familyService);

// 加载关联实体（家庭成员和家庭住址）
family.loadRelated(FamilyDomain.FamilyMemberDomain.class);
family.loadRelated(FamilyDomain.FamilyAddressDomain.class);

// 访问成员及其引用的住址数据
for (FamilyDomain.FamilyMemberDomain member : family.getFamilyMemberList()) {
    // 访问 ref 数据（自动从 familyAddressList 中懒加载）
    FamilyAddressDomain address = member.getRefAddress();
    
    if (address != null) {
        System.out.println("成员: " + member.getName() + 
                          ", 住址: " + address.getAddressName());
    }
}
```

**工作原理**:
1. ref 数据不需要单独查询，从父聚合根的 related 列表中过滤获取
2. 使用 `CacheDomain` 包装，实现懒加载和缓存
3. 通过 Lambda 表达式匹配外键关系
4. 线程安全（synchronized 方法）

### 4.3 外键映射规则详解

`fk="id:family_id"` 的含义：

```
主表字段 (id) : 关联表外键字段 (family_id)
     ↓                    ↓
  family.ID        family_member.family_id
```

**SQL 等价**:
```sql
SELECT * FROM family_member 
WHERE family_member.family_id = family.ID
```

### 4.5 冗余字段映射规则

`redundancy="name:family_name"` 的含义：

```
主表字段 (name) → 关联表冗余字段 (family_name)
     ↓                      ↓
  family.name         family_member.family_name
```

**框架自动处理**:
```java
// 插入 family_member 时，自动填充 family_name
FamilyMemberDomain member = new FamilyMemberDomain();
member.setFamilyId(1L);
member.setName("张三");
// member.setFamilyName("张氏家族");  ← 不需要手动设置！

familyService.insert(family);  // 自动从 family.name 填充到 member.familyName
```

### 4.4 完整配置示例

```xml
<?xml version="1.0" encoding="UTF-8"?>
<domains>
    <!-- 家庭领域模型（包含关联和引用关系） -->
    <domain name="family" 
            description="家庭领域模型" 
            main-table="family">
        
        <!-- 家庭成员（一对多关联） -->
        <related description="家庭成员" 
                 table="family_member" 
                 many="true" 
                 fk="id:family_id" 
                 redundancy="name:family_name">
            
            <!-- 引用：成员绑定的住址（一对一引用） -->
            <ref name="address" 
                 description="成员住址绑定"
                 table="family_address" 
                 many="false" 
                 fk="id:family_member_id"/>
        </related>
        
        <!-- 家庭住址（一对多关联） -->
        <related description="家庭住址" 
                 table="family_address" 
                 many="true" 
                 fk="id:family_id" 
                 redundancy="name:family_name"/>
    </domain>
</domains>
```

**文件位置**: `domain-{name}.xml`

---

## 4. 代码生成流程

> ⚠️ **规则**: 使用 `ddd-code-generator` 技能生成领域层代码，**禁止**手动编写或修改生成的代码。

### 5.1 代码生成工作流

```
┌─────────────────────────────────────────────────────────┐
│  第 1 步: 准备 SQL DDL 文件                              │
└────────────────────┬────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────┐
│  第 2 步: 编写 domain-{name}.xml                        │
└────────────────────┬────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────┐
│  第 3 步: 调用 ddd-code-generator 技能                   │
│  🛠️ Skill: ddd-code-generator                          │
│  输入: SQL DDL + domain-{name}.xml                      │
│  输出: 完整的领域层代码                                  │
└────────────────────┬────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────┐
│  第 4 步: 验证生成的代码                                 │
│  ✅ 检查生成的 Domain/Service/Repository 等类           │
│  ✅ 确认编译通过                                        │
└────────────────────┬────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────┐
│  第 5 步: 在 AppService 层编写业务逻辑                   │
│  ✅ 不修改生成的代码                                    │
│  ✅ 在应用层扩展业务功能                                │
└─────────────────────────────────────────────────────────┘
```

### 5.2 使用 ddd-code-generator 技能

#### 调用方式

```
使用技能: ddd-code-generator

输入参数:
- domain_xml: domain-{name}.xml 文件路径
- sql_file: SQL DDL 文件路径（MySQL 或 PostgreSQL）
- output_dir: 代码输出目录
- base_package: Java 基础包名（如 com.example.project）
- dialect: 数据库类型（mysql 或 postgresql）
```

#### 示例调用

```
请帮我生成家庭领域模型的代码：
- domain XML: domain-family.xml
- SQL DDL: family-mysql.sql
- 输出目录: src/main/java
- 基础包名: com.artframework.sample
- 数据库类型: mysql
```

### 5.3 生成的代码结构

```
{base_package}/
├── domains/                                    # 领域层
│   └── family/                                 # 家庭领域
│       ├── domain/
│       │   ├── FamilyDomain.java              # 聚合根（包含内部类：关联实体）
│       │   └── FamilyFindDomain.java          # 查询请求 DTO
│       │
│       ├── repository/                         # 仓储接口
│       │   ├── FamilyRepository.java          # 聚合根 Repository
│       │   └── impl/
│       │       ├── FamilyRepositoryImpl.java  # Repository 实现
│       │       └── FamilyMemberRepositoryImpl.java
│       │
│       ├── service/                            # 领域服务接口
│       │   ├── FamilyService.java
│       │   └── impl/
│       │       └── FamilyServiceImpl.java     # 领域服务实现
│       │
│       ├── convertor/                          # 对象转换器（MapStruct）
│       │   ├── FamilyConvertor.java
│       │   └── FamilyConvertorDecorator.java
│       │
│       └── lambdaexp/                          # Lambda 表达式
│           └── FamilyLambdaExp.java
│
├── entities/                                   # 数据库实体（DO）
│   ├── FamilyDO.java
│   ├── FamilyAddressDO.java
│   └── FamilyMemberDO.java
│
├── mappers/                                    # MyBatis Mapper
│   ├── FamilyMapper.java
│   ├── FamilyAddressMapper.java
│   └── FamilyMemberMapper.java
│
└── controllers/                                # REST 控制器
    └── FamilyController.java
```

### 5.4 生成代码详解

#### 5.4.1 聚合根类（FamilyDomain.java）

```java
@Schema(description = "家庭领域模型")
@NoArgsConstructor
@AllArgsConstructor
public class FamilyDomain extends BaseAggregateDomain<FamilyDomain, FamilyService> {
    
    // ===== 聚合根自身字段 =====
    @Getter @Setter
    private Long id;
    
    @Getter @Setter
    private String name;
    
    @Getter @Setter
    private Integer personCount;
    
    @Getter @Setter
    private String householder;
    
    // ===== 一对一关联实体 =====
    private FamilyAddressDomain familyAddress;
    
    // ===== 一对多关联实体 =====
    private List<FamilyMemberDomain> familyMemberList;
    
    // ===== 静态加载方法 =====
    public static FamilyDomain load(Serializable key, FamilyService service) {
        FamilyDomain domain = service.find(
            FamilyFindDomain.builder().key(key).build()
        );
        if (ObjectUtil.isNotNull(domain)) {
            domain._service = service;
        }
        return domain;
    }
    
    // ===== 内部类：一对一关联实体 =====
    @NoArgsConstructor
    @AllArgsConstructor
    public static class FamilyAddressDomain extends BaseDomain {
        @Getter @Setter
        private Long id;
        @Getter @Setter
        private Long familyId;
        @Getter @Setter
        private String familyName;  // 冗余字段
        @Getter @Setter
        private String addressName;
    }
    
    // ===== 内部类：一对多关联实体 =====
    @NoArgsConstructor
    @AllArgsConstructor
    public static class FamilyMemberDomain extends BaseDomain {
        @Getter @Setter
        private Long id;
        @Getter @Setter
        private Long familyId;
        @Getter @Setter
        private String familyName;  // 冗余字段
        @Getter @Setter
        private String name;
        @Getter @Setter
        private String phone;
        @Getter @Setter
        private String type;
    }
}
```

#### 5.4.2 Repository 接口

```java
public interface FamilyRepository extends BaseRepository<FamilyDomain, FamilyDO> {
    // 继承 BaseRepository 的所有方法：
    // - query(): 查询单个
    // - queryList(): 查询列表
    // - queryPage(): 分页查询
    // - insert(): 插入
    // - update(): 更新
    // - delete(): 删除
}
```

#### 5.4.3 Service 接口

```java
public interface FamilyService extends BaseDomainService {
    /**
     * 根据条件查找家庭（包含关联数据）
     */
    FamilyDomain find(FamilyFindDomain request);
    
    /**
     * 插入家庭（级联插入关联实体）
     * @return 插入后的 ID
     */
    Integer insert(FamilyDomain domain);
    
    /**
     * 更新家庭（增量更新，只更新变化的字段）
     */
    void update(FamilyDomain newDomain, FamilyDomain oldDomain);
    
    /**
     * 删除家庭（级联删除关联实体）
     */
    Boolean delete(Serializable id);
    
    // 继承 BaseDomainService 的方法：
    // - queryOne(): 查询单个
    // - queryList(): 查询列表
    // - queryPage(): 分页查询
}
```

### 5.5 代码生成的命名规则

| 配置值 | 生成的类名 | 说明 |
|--------|-----------|------|
| `name="family"` | `FamilyDomain` | 聚合根类 |
| `table="family"` | `FamilyDO` | 数据库实体 |
| `table="family"` | `FamilyMapper` | MyBatis Mapper |
| `table="family"` | `FamilyRepository` | Repository 接口 |
| `table="family"` | `FamilyService` | Service 接口 |
| `table="family_address"` | `FamilyDomain.FamilyAddressDomain` | 内部类：关联实体 |

### 5.6 ⚠️ 代码生成约束规则

> 🚨 **极其重要**: 以下规则必须严格遵守！

#### 禁止操作

```
❌ 禁止手动修改以下生成的文件：
   - *Domain.java（聚合根和关联实体）
   - *Service.java（领域服务接口）
   - *ServiceImpl.java（领域服务实现）
   - *Repository.java（仓储接口）
   - *RepositoryImpl.java（仓储实现）
   - *Mapper.java（MyBatis Mapper）
   - *Convertor.java（对象转换器）
   - *LambdaExp.java（Lambda 表达式）
```

#### 正确做法

```
✅ 需要修改领域结构时：
   1. 修改 config/domain-config.xml
   2. 重新运行 ddd-code-generator 技能
   3. 生成器会自动覆盖已存在的文件

✅ 需要扩展业务逻辑时：
   1. 在 AppService 层编写新的业务方法
   2. 调用生成的 DomainService 提供的基础 CRUD 方法
   3. 不要修改生成的 DomainService 代码
```

---

## 5. 领域模型使用指南

> ⚠️ **规则**: 在 AppService 层使用生成的领域模型，遵循以下操作规范。

### 6.1 加载领域对象

#### 基本加载

```java
@Autowired
private FamilyService familyService;

// 通过 ID 加载聚合根
FamilyDomain family = FamilyDomain.load(1L, familyService);
```

#### 加载关联数据

```java
// ✅ 方式1：加载所有关联数据
FamilyDomain family = FamilyDomain.load(1L, familyService);
family.loadRelated(FamilyDomain.FamilyAddressDomain.class);
family.loadRelated(FamilyDomain.FamilyMemberDomain.class);

// ✅ 方式2：带条件加载（推荐）
family.loadRelated(FamilyDomain.FamilyMemberDomain.class, query -> {
    query.eq(FamilyMemberDomain::getType, "父亲")
         .orderBy(FamilyMemberDomain::getCreateTime, Order.DESC);
});

// ✅ 方式3：使用 LambdaQuery 加载
LambdaQuery<FamilyDomain.FamilyMemberDomain> memberQuery = 
    LambdaQuery.of(FamilyDomain.FamilyMemberDomain.class);
memberQuery.eq(FamilyDomain.FamilyMemberDomain::getFamilyId, 1L);
family.loadRelated(FamilyDomain.FamilyMemberDomain.class, memberQuery);

// 访问关联数据
FamilyAddressDomain address = family.getFamilyAddress();
List<FamilyMemberDomain> members = family.getFamilyMemberList();
```

### 6.2 插入操作

#### 插入聚合根（包含关联实体）

```java
// 1. 创建聚合根
FamilyDomain family = new FamilyDomain();
family.setName("张氏家族");
family.setPersonCount(3);
family.setHouseholder("张三");

// 2. 创建关联实体（一对一）
FamilyDomain.FamilyAddressDomain address = new FamilyDomain.FamilyAddressDomain();
address.setAddressName("北京市朝阳区XX路XX号");
family.setFamilyAddress(address);

// 3. 创建关联实体列表（一对多）
List<FamilyDomain.FamilyMemberDomain> members = new ArrayList<>();

FamilyDomain.FamilyMemberDomain member1 = new FamilyDomain.FamilyMemberDomain();
member1.setName("张三");
member1.setPhone("13800138000");
member1.setType("户主");
members.add(member1);

FamilyDomain.FamilyMemberDomain member2 = new FamilyDomain.FamilyMemberDomain();
member2.setName("李四");
member2.setPhone("13800138001");
member2.setType("配偶");
members.add(member2);

family.setFamilyMemberList(members);

// 4. 执行插入
// ✅ 框架自动处理：
//    - 插入 family 表，获取 ID
//    - 填充关联实体的 familyId 和 familyName（冗余字段）
//    - 插入 family_address 表
//    - 插入 family_member 表
//    - 回填所有实体的 ID
Integer familyId = familyService.insert(family);

// 插入后，所有实体的 ID 都会自动回填
System.out.println("Family ID: " + family.getId());
System.out.println("Member1 ID: " + member1.getId());
System.out.println("Member2 ID: " + member2.getId());
```

### 6.3 更新操作

#### 增量更新（推荐）

> 💡 **核心特性**: 框架自动对比新旧对象，只更新发生变化的字段。

```java
// 1. 加载原始数据
FamilyDomain oldFamily = FamilyDomain.load(1L, familyService);
oldFamily.loadRelated(FamilyDomain.FamilyMemberDomain.class);

// 2. 创建新对象（可以从前端传入的 DTO 转换）
FamilyDomain newFamily = new FamilyDomain();
newFamily.setId(1L);
newFamily.setName("张氏大家族");  // 修改了名称
newFamily.setPersonCount(5);      // 修改了人数

// 3. 执行增量更新
// ✅ 框架自动对比，只更新 name 和 person_count 字段
familyService.update(newFamily, oldFamily);
```

#### 更新关联实体

```java
// 1. 加载原始数据
FamilyDomain oldFamily = FamilyDomain.load(1L, familyService);
oldFamily.loadRelated(FamilyDomain.FamilyAddressDomain.class);
oldFamily.loadRelated(FamilyDomain.FamilyMemberDomain.class);

// 2. 修改一对一关联实体
FamilyDomain.FamilyAddressDomain newAddress = new FamilyDomain.FamilyAddressDomain();
newAddress.setId(oldFamily.getFamilyAddress().getId());
newAddress.setAddressName("上海市浦东新区XX路XX号");  // 修改地址
oldFamily.setFamilyAddress(newAddress);

// 3. 修改一对多关联实体（MERGE 策略）
List<FamilyDomain.FamilyMemberDomain> newMembers = new ArrayList<>();
FamilyDomain.FamilyMemberDomain newMember = new FamilyDomain.FamilyMemberDomain();
newMember.setName("王五");
newMember.setType("孩子");
newMembers.add(newMember);
oldFamily.setFamilyMemberList(newMembers);

// 4. 执行更新
// ✅ 框架自动处理：
//    - 更新 family_address 表（UPDATE）
//    - 删除旧的 family_member 记录（DELETE）
//    - 插入新的 family_member 记录（INSERT）
familyService.update(oldFamily, oldFamily);
```

### 6.4 删除操作

#### 删除聚合根（级联删除）

```java
// ✅ 方式1：删除聚合根，自动级联删除所有关联实体
Boolean success = familyService.delete(1L);
// 自动删除：family_address 和 family_member 的相关记录

// ✅ 方式2：指定加载标志，控制级联删除范围
FamilyDomain.LoadFlag loadFlag = new FamilyDomain.LoadFlag()
    .loadFamilyMemberList();  // 只删除成员，不删除地址
familyService.delete(1L, loadFlag);
```

### 6.5 查询操作

#### 使用 LambdaQuery 查询

```java
// 构建查询条件（类型安全，编译期检查）
LambdaQuery<FamilyDomain> query = LambdaQuery.of(FamilyDomain.class);
query.eq(FamilyDomain::getName, "张氏家族")
     .gt(FamilyDomain::getPersonCount, 2)
     .orderBy(FamilyDomain::getCreateTime, Order.DESC);

// ✅ 查询单个
FamilyDomain family = familyService.queryOne(FamilyDomain.class, query);

// ✅ 查询列表
List<FamilyDomain> families = familyService.queryList(FamilyDomain.class, query);

// ✅ 分页查询
PageDomain pageDomain = new PageDomain();
pageDomain.setPageNum(1);
pageDomain.setPageSize(10);
IPage<FamilyDomain> page = familyService.queryPage(
    FamilyDomain.class, 
    pageDomain, 
    query
);
```

#### 复杂查询（AND/OR 嵌套）

```java
LambdaQuery<FamilyDomain> query = LambdaQuery.of(FamilyDomain.class);

// ✅ 示例1：(name = '张氏家族' OR name = '李氏家族') AND person_count > 2
query.eq(FamilyDomain::getName, "张氏家族")
     .or()
     .eq(FamilyDomain::getName, "李氏家族")
     .gt(FamilyDomain::getPersonCount, 2);

// ✅ 示例2：name = '张氏家族' AND (person_count > 2 OR householder IS NOT NULL)
query.eq(FamilyDomain::getName, "张氏家族")
     .and(q -> q
         .gt(FamilyDomain::getPersonCount, 2)
         .or()
         .isNotNull(FamilyDomain::getHouseholder)
     );
```

#### 常用查询操作符

| 操作符 | 说明 | SQL 等价 | 示例 |
|--------|------|---------|------|
| `eq` | 等于 | `=` | `query.eq(FamilyDomain::getName, "张氏")` |
| `ne` | 不等于 | `!=` | `query.ne(FamilyDomain::getName, "测试")` |
| `gt` | 大于 | `>` | `query.gt(FamilyDomain::getPersonCount, 2)` |
| `ge` | 大于等于 | `>=` | `query.ge(FamilyDomain::getPersonCount, 2)` |
| `lt` | 小于 | `<` | `query.lt(FamilyDomain::getPersonCount, 10)` |
| `le` | 小于等于 | `<=` | `query.le(FamilyDomain::getPersonCount, 10)` |
| `like` | 模糊匹配 | `LIKE '%value%'` | `query.like(FamilyDomain::getName, "张")` |
| `likeLeft` | 左模糊 | `LIKE '%value'` | `query.likeLeft(FamilyDomain::getName, "家族")` |
| `likeRight` | 右模糊 | `LIKE 'value%'` | `query.likeRight(FamilyDomain::getName, "张氏")` |
| `in` | 包含 | `IN (...)` | `query.in(FamilyDomain::getId, Arrays.asList(1L, 2L))` |
| `notIn` | 不包含 | `NOT IN (...)` | `query.notIn(FamilyDomain::getId, Arrays.asList(1L))` |
| `isNull` | 为空 | `IS NULL` | `query.isNull(FamilyDomain::getHouseholder)` |
| `notNull` | 不为空 | `IS NOT NULL` | `query.notNull(FamilyDomain::getName)` |

### 6.6 事务管理

```java
@Service
public class FamilyAppService {
    
    @Autowired
    private FamilyService familyService;
    
    /**
     * ✅ 在应用服务层添加 @Transactional 注解
     */
    @Transactional(rollbackFor = Exception.class)
    public void createFamilyWithMembers(FamilyDTO familyDTO) {
        // 1. 转换为领域对象
        FamilyDomain family = FamilyConvertor.INSTANCE.toDomain(familyDTO);
        
        // 2. 插入（自动处理关联实体）
        familyService.insert(family);
        
        // 3. 其他业务操作...
    }
}
```

---

## 5.7 LoadFlag 与 Query 查询控制

> 💡 **核心功能**: LoadFlag 控制关联数据的加载行为，Query 控制查询参数和排序。

### 5.7.1 LoadFlag 作用

`LoadFlag` 用于控制**加载哪些关联实体**，每个聚合根自动生成对应的 LoadFlag 类：

```java
// FamilyDomain.LoadFlag
public static class LoadFlag extends BaseLoadFlag {
    private Boolean loadFamilyMemberDomain;    // 是否加载家庭成员
    private Boolean loadFamilyAddressDomain;   // 是否加载家庭住址
}
```

### 5.7.2 Query 查询参数

`BaseLoadFlag.Query` 提供对每个关联实体的查询控制：

| 属性 | 类型 | 说明 |
|------|------|------|
| `filter` | LambdaQuery.ConditionGroup | 过滤条件（AND/OR 组合） |
| `order` | List<LambdaOrderItem> | 排序规则 |
| `select` | List<String> | 字段选择（白名单） |
| `exclude` | List<String> | 字段排除（黑名单） |

### 5.7.3 API 调用示例

**场景**: 查询家庭信息，只加载状态为启用的家庭成员，按姓名排序

```json
POST /api/family/find
{
  "key": 1,
  "loadFlag": {
    "loadFamilyMemberDomain": true,
    "loadFamilyAddressDomain": false,
    "query": {
      "FamilyMemberDomain": {
        "filter": {
          "logic": "AND",
          "condition": [
            {
              "field": "status",
              "op": "EQ",
              "value": 1
            },
            {
              "field": "type",
              "op": "IN",
              "value": ["父亲", "母亲"]
            }
          ]
        },
        "order": [
          {
            "field": "name",
            "order": "ASC"
          }
        ],
        "select": ["id", "name", "type", "phone"]
      }
    }
  }
}
```

**响应结果**:
```json
{
  "id": 1,
  "name": "张氏家族",
  "familyMemberList": [
    {
      "id": 10,
      "name": "张三",
      "type": "父亲",
      "phone": "13800138000"
    },
    {
      "id": 11,
      "name": "李四",
      "type": "母亲",
      "phone": "13800138001"
    }
  ],
  "familyAddressList": null
}
```

### 5.7.4 代码中使用示例

```java
// AppService 中构建查询
FamilyFindDomain findDomain = FamilyFindDomain.builder()
    .key(1L)
    .loadFlag(new FamilyDomain.LoadFlag() {{
        setLoadFamilyMemberDomain(true);
        
        // 配置 Query
        BaseLoadFlag.Query memberQuery = new BaseLoadFlag.Query();
        
        // 过滤条件
        LambdaQuery<FamilyDomain.FamilyMemberDomain> query = 
            LambdaQuery.of(FamilyDomain.FamilyMemberDomain.class);
        query.eq(FamilyDomain.FamilyMemberDomain::getStatus, 1)
             .in(FamilyDomain.FamilyMemberDomain::getType, 
                 Arrays.asList("父亲", "母亲"));
        memberQuery.setFilter(query.getFilter());
        
        // 排序
        memberQuery.setOrder(Arrays.asList(
            new LambdaOrderItem(FamilyDomain.FamilyMemberDomain::getName, Order.ASC)
        ));
        
        // 字段选择
        memberQuery.setSelect(Arrays.asList("id", "name", "type", "phone"));
        
        getQuery().put("FamilyMemberDomain", memberQuery);
    }})
    .build();

FamilyDomain family = familyService.find(findDomain);
```

### 5.7.5 关键要点

- ✅ **不配置 Query**：加载关联实体的全部数据
- ✅ **filter**：支持 `eq`, `ne`, `gt`, `lt`, `like`, `in`, `isNull` 等操作符，可 AND/OR 嵌套
- ✅ **order**：支持多字段排序，使用 `orderBy` 和 `thenBy`
- ✅ **select/exclude**：控制返回字段，优化数据传输
- ⚠️ **LoadFlag 可合并**：使用 `LoadFlag.merge()` 合并多个配置

---

## 6. 分层架构规范

> ⚠️ **规则**: 严格遵守分层架构调用链，禁止跨层调用。

### 7.1 分层架构调用链

```
┌─────────────────────────────────────────────────────────┐
│  Controller 层                                          │
│  📍 职责: 接收 HTTP 请求，参数校验，返回响应              │
│  📍 调用: AppService                                    │
│  🚫 禁止: 直接调用 DomainService/Repository/Mapper      │
└────────────────────┬────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────┐
│  AppService 层（应用服务）                               │
│  📍 职责: 编排业务逻辑，事务管理，跨领域协调              │
│  📍 调用: DomainService                                 │
│  🚫 禁止: 直接调用 Repository/Mapper                    │
└────────────────────┬────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────┐
│  DomainService 层（领域服务）✅ 自动生成                  │
│  📍 职责: 领域对象的 CRUD，聚合根管理                   │
│  📍 调用: Repository                                    │
│  🚫 禁止: 手动修改生成的代码                            │
└────────────────────┬────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────┐
│  Repository 层（仓储）✅ 自动生成                         │
│  📍 职责: 数据访问抽象，ORM 映射                        │
│  📍 调用: Mapper                                        │
│  🚫 禁止: 手动修改生成的代码                            │
└────────────────────┬────────────────────────────────────┘
                     ↓
┌─────────────────────────────────────────────────────────┐
│  Mapper 层（MyBatis）✅ 自动生成                          │
│  📍 职责: SQL 执行                                      │
│  📍 调用: 数据库                                        │
│  🚫 禁止: 手动修改生成的代码                            │
└─────────────────────────────────────────────────────────┘
```

### 7.2 各层职责详解

#### Controller 层

```java
@RestController
@RequestMapping("/api/family")
public class FamilyController {
    
    @Autowired
    private FamilyAppService familyAppService;  // ✅ 调用 AppService
    
    @GetMapping("/{id}")
    public FamilyDomain getFamily(@PathVariable Long id) {
        // ✅ 只负责接收请求和返回结果
        return familyAppService.getFamilyById(id);
    }
    
    @PostMapping
    public Integer createFamily(@RequestBody FamilyDTO familyDTO) {
        // ✅ 参数校验由 Spring 自动处理
        return familyAppService.createFamily(familyDTO);
    }
}
```

**职责**:
- ✅ 接收 HTTP 请求
- ✅ 参数校验（使用 `@Valid`）
- ✅ 返回响应结果
- 🚫 **禁止**: 包含业务逻辑
- 🚫 **禁止**: 直接调用 DomainService/Repository

#### AppService 层（应用服务）

```java
@Service
public class FamilyAppService {
    
    @Autowired
    private FamilyService familyService;  // ✅ 调用 DomainService
    
    @Autowired
    private OrderService orderService;    // ✅ 可以协调多个 DomainService
    
    /**
     * ✅ 事务管理在 AppService 层
     */
    @Transactional(rollbackFor = Exception.class)
    public Integer createFamily(FamilyDTO familyDTO) {
        // 1. DTO 转换为 Domain
        FamilyDomain family = FamilyConvertor.INSTANCE.toDomain(familyDTO);
        
        // 2. 调用 DomainService 执行插入
        Integer familyId = familyService.insert(family);
        
        // 3. 其他业务操作（如发送通知、记录日志等）
        sendNotification(family);
        
        return familyId;
    }
    
    /**
     * ✅ 业务编排
     */
    @Transactional(rollbackFor = Exception.class)
    public void transferFamilyMembers(Long fromFamilyId, Long toFamilyId, List<Long> memberIds) {
        // 1. 加载两个家庭
        FamilyDomain fromFamily = FamilyDomain.load(fromFamilyId, familyService);
        fromFamily.loadRelated(FamilyDomain.FamilyMemberDomain.class);
        
        FamilyDomain toFamily = FamilyDomain.load(toFamilyId, familyService);
        toFamily.loadRelated(FamilyDomain.FamilyMemberDomain.class);
        
        // 2. 业务逻辑：转移成员
        // ...
        
        // 3. 保存
        familyService.update(fromFamily, fromFamily);
        familyService.update(toFamily, toFamily);
    }
}
```

**职责**:
- ✅ 业务逻辑编排
- ✅ 事务管理（`@Transactional`）
- ✅ 跨领域协调
- ✅ DTO 与 Domain 转换
- 🚫 **禁止**: 直接调用 Repository/Mapper

#### DomainService 层（领域服务）✅ 自动生成

```java
// ✅ 这是生成的代码，不要手动修改！
public interface FamilyService extends BaseDomainService {
    FamilyDomain find(FamilyFindDomain request);
    Integer insert(FamilyDomain domain);
    void update(FamilyDomain newDomain, FamilyDomain oldDomain);
    Boolean delete(Serializable id);
    
    // 继承的基础方法
    <T extends BaseDomain> T queryOne(Class<T> clazz, LambdaQuery<T> lambdaQuery);
    <T extends BaseDomain> List<T> queryList(Class<T> clazz, LambdaQuery<T> lambdaQuery);
    <T extends BaseDomain> IPage<T> queryPage(Class<T> clazz, PageDomain pageDomain, LambdaQuery<T> lambdaQuery);
}
```

**职责**:
- ✅ 领域对象的 CRUD 操作
- ✅ 聚合根管理（级联插入、更新、删除）
- ✅ 增量更新（自动对比字段变化）
- 🚫 **禁止**: 手动修改生成的代码

### 7.3 ❌ 错误示例 vs ✅ 正确示例

#### 错误：Controller 直接调用 Repository

```java
// ❌ 错误示例
@RestController
public class FamilyController {
    @Autowired
    private FamilyRepository familyRepository;  // ❌ 禁止直接调用 Repository
    
    @GetMapping("/family/{id}")
    public FamilyDomain getFamily(@PathVariable Long id) {
        return familyRepository.query(id, FamilyLambdaExp::getId);
    }
}
```

#### 正确：遵循分层调用链

```java
// ✅ 正确示例
@RestController
public class FamilyController {
    @Autowired
    private FamilyAppService familyAppService;  // ✅ 调用 AppService
    
    @GetMapping("/family/{id}")
    public FamilyDomain getFamily(@PathVariable Long id) {
        return familyAppService.getFamilyById(id);
    }
}

@Service
public class FamilyAppService {
    @Autowired
    private FamilyService familyService;  // ✅ AppService 调用 DomainService
    
    public FamilyDomain getFamilyById(Long id) {
        return FamilyDomain.load(id, familyService);
    }
}
```

### 8.7 日志规范

---

## 8. 最佳实践

> ⚠️ **规则**: 遵循以下最佳实践，确保代码质量和框架特性的充分利用。



#### ✅ 使用类型安全的 Lambda 查询

```java
// ✅ 推荐：类型安全，编译期检查
LambdaQuery<FamilyDomain> query = LambdaQuery.of(FamilyDomain.class);
query.eq(FamilyDomain::getName, "张氏家族");

// ❌ 不推荐：字符串字段名，容易出错，没有编译期检查
query.eq("name", "张氏家族");
```

#### ✅ 避免 N+1 查询问题

```java
// ❌ 错误：循环中查询数据库（N+1 问题）
for (Long id : ids) {
    FamilyDomain family = FamilyDomain.load(id, familyService);
    family.loadRelated(FamilyDomain.FamilyMemberDomain.class);  // 每次循环都查询数据库
}

// ✅ 正确：批量查询
LambdaQuery<FamilyDomain> query = LambdaQuery.of(FamilyDomain.class);
query.in(FamilyDomain::getId, ids);
List<FamilyDomain> families = familyService.queryList(FamilyDomain.class, query);

// 一次性加载所有关联数据
for (FamilyDomain family : families) {
    family.loadRelated(FamilyDomain.FamilyMemberDomain.class);
}
```

#### ✅ 分页查询限制

```java
// 框架默认限制最大 10000 条记录
PageDomain pageDomain = new PageDomain();
pageDomain.setPageSize(100);     // ✅ 合理
pageDomain.setPageSize(20000);   // ❌ 超过限制会被自动调整
```

### 8.1 查询最佳实践

#### ✅ 使用框架提供的批量方法

```java
List<FamilyDomain> families = Arrays.asList(family1, family2, family3);

// ✅ 批量插入（框架自动优化 SQL）
families.forEach(family -> familyService.insert(family));

// ✅ 批量删除
ids.forEach(id -> familyService.delete(id));
```

#### 数据库特定的批量优化

框架自动根据数据库类型选择最优的批量操作策略：

| 数据库 | 批量插入优化 | 批量更新优化 |
|--------|-------------|-------------|
| MySQL | `INSERT ... VALUES (...), (...), ...` | `ON DUPLICATE KEY UPDATE` |
| PostgreSQL | `INSERT INTO ... VALUES (...), (...), ...` | `ON CONFLICT ... DO UPDATE` |
| Oracle | `INSERT ALL INTO ...` | `MERGE INTO ... USING` |

### 8.2 批量操作优化

```java
// 1️⃣ 加载
FamilyDomain family = FamilyDomain.load(1L, familyService);

// 2️⃣ 加载关联数据（按需加载）
family.loadRelated(FamilyDomain.FamilyMemberDomain.class);

// 3️⃣ 修改
family.setName("新名称");
family.setChanged(true);  // 标记为已修改（框架自动处理）

// 4️⃣ 保存（增量更新）
FamilyDomain oldFamily = ...;  // 原始数据
familyService.update(family, oldFamily);

// 5️⃣ 删除（级联删除）
familyService.delete(1L);
```

### 8.3 领域对象生命周期管理

领域对象已配置序列化规则，以下字段**不会**序列化到前端：

```java
// 自动忽略的字段：
- _service              // 领域服务引用
- _thisDomain           // 内部引用
- @JsonIgnore 标注的字段
- @PropIgnore 标注的字段
```

```java
// ✅ 可以直接将领域对象返回给前端
@GetMapping("/family/{id}")
public FamilyDomain getFamily(@PathVariable Long id) {
    return FamilyDomain.load(id, familyService);
    // 自动忽略 _service 等内部字段
}
```

### 8.4 序列化控制

```java
@Service
@Slf4j
public class FamilyAppService {
    
    @Autowired
    private FamilyService familyService;
    
    public Integer createFamily(FamilyDTO dto) {
        log.info("开始创建家庭: name={}", dto.getName());
        
        try {
            // 1. DTO 转换为 Domain
            FamilyDomain family = FamilyConvertor.INSTANCE.toDomain(dto);
            
            // 2. 执行插入
            Integer id = familyService.insert(family);
            
            log.info("家庭创建成功: id={}", id);
            return id;
            
        } catch (Exception e) {
            log.error("家庭创建失败: name={}", dto.getName(), e);
            // 抛出自定义业务异常
            throw new BusinessException("创建家庭失败: " + e.getMessage(), e);
        }
    }
}
```

### 8.5 异常处理规范

```java
@SpringBootTest
@Slf4j
class FamilyServiceTest {
    
    @Autowired
    private FamilyService familyService;
    
    @Test
    @Transactional
    @Rollback  // 测试完成后自动回滚
    void testInsertFamily() {
        // 1. 准备数据
        FamilyDomain family = new FamilyDomain();
        family.setName("测试家庭");
        family.setPersonCount(3);
        
        // 2. 执行插入
        Integer id = familyService.insert(family);
        
        // 3. 验证结果
        assertNotNull(id);
        log.info("插入成功，ID: {}", id);
        
        // 4. 验证加载
        FamilyDomain loaded = FamilyDomain.load(id, familyService);
        assertEquals("测试家庭", loaded.getName());
        assertEquals(3, loaded.getPersonCount());
    }
    
    @Test
    @Transactional
    @Rollback
    void testUpdateFamily() {
        // 1. 插入测试数据
        FamilyDomain family = new FamilyDomain();
        family.setName("原始名称");
        family.setPersonCount(2);
        Integer id = familyService.insert(family);
        
        // 2. 加载原始数据
        FamilyDomain oldFamily = FamilyDomain.load(id, familyService);
        
        // 3. 创建新对象（修改部分字段）
        FamilyDomain newFamily = new FamilyDomain();
        newFamily.setId(id);
        newFamily.setName("新名称");
        newFamily.setPersonCount(5);
        
        // 4. 执行增量更新
        familyService.update(newFamily, oldFamily);
        
        // 5. 验证更新结果
        FamilyDomain updated = FamilyDomain.load(id, familyService);
        assertEquals("新名称", updated.getName());
        assertEquals(5, updated.getPersonCount());
    }
}
```

### 8.6 单元测试规范

```java
@Service
@Slf4j
public class FamilyAppService {
    
    // ✅ INFO 级别：记录关键业务操作
    public Integer createFamily(FamilyDTO dto) {
        log.info("开始创建家庭: name={}", dto.getName());
        // ...
        log.info("家庭创建成功: id={}", id);
        return id;
    }
    
    // ✅ ERROR 级别：记录异常和错误
    public void deleteFamily(Long id) {
        try {
            familyService.delete(id);
            log.info("家庭删除成功: id={}", id);
        } catch (Exception e) {
            log.error("家庭删除失败: id={}", id, e);
            throw e;
        }
    }
    
    // ✅ DEBUG 级别：记录详细的调试信息
    public List<FamilyDomain> searchFamilies(String keyword) {
        log.debug("搜索家庭: keyword={}", keyword);
        // ...
        log.debug("搜索结果: count={}", families.size());
        return families;
    }
}
```

---

## 7. 框架适用场景与限制

> ⚠️ **重要**: 了解框架的边界，在合适的场景使用合适的工具。

### 8.1 DDD 框架的优势场景

DDD 框架在以下场景中表现优秀：

| 场景 | 说明 | 示例 |
|------|------|------|
| **单聚合根 CRUD** | 单个聚合根及其关联实体的增删改查 | 家庭的创建、查询、更新、删除 |
| **聚合根管理** | 聚合根自动管理关联实体的生命周期 | 插入家庭时自动插入成员和地址 |
| **增量更新** | 自动对比新旧对象，只更新变化的字段 | 只更新家庭名称，不更新其他字段 |
| **级联操作** | 自动级联插入、更新、删除关联实体 | 删除家庭时自动删除成员和地址 |
| **类型安全查询** | Lambda 表达式提供编译期类型检查 | `query.eq(FamilyDomain::getName, "张氏")` |
| **懒加载** | 按需加载关联数据，优化性能 | 只加载家庭成员，不加载地址 |

### 8.2 DDD 框架的短板

在以下场景中，**不建议使用 DDD 框架**，应该直接使用 MyBatis 编写自定义 SQL：

#### 8.2.1 多表关联查询（JOIN）

**问题**: DDD 框架不支持跨聚合根的 JOIN 查询

```java
// ❌ 不适用：需要 JOIN 多个表
// 查询家庭成员及其住址信息（跨表 JOIN）
SELECT 
    m.id, m.name, m.phone,
    a.address_name
FROM family_member m
LEFT JOIN family_address a ON m.family_id = a.family_id
WHERE m.type = '父亲'

// ✅ 正确做法：使用 MyBatis 自定义 Mapper
@Mapper
public interface FamilyMemberCustomMapper {
    @Select("""
        SELECT 
            m.id, m.name, m.phone, m.type,
            a.address_name
        FROM family_member m
        LEFT JOIN family_address a ON m.family_id = a.family_id
        WHERE m.type = #{type}
    """)
    List<MemberAddressDTO> selectMembersWithAddress(@Param("type") String type);
}
```

#### 8.2.2 复杂统计查询

**问题**: DDD 框架不支持 GROUP BY、聚合函数等复杂统计

```java
// ❌ 不适用：需要 GROUP BY 和聚合函数
// 统计每个家庭的成员数量
SELECT 
    f.id, f.name,
    COUNT(m.id) as member_count,
    AVG(m.age) as avg_age
FROM family f
LEFT JOIN family_member m ON f.id = m.family_id
GROUP BY f.id, f.name

// ✅ 正确做法：使用 MyBatis 自定义 Mapper
@Mapper
public interface FamilyCustomMapper {
    @Select("""
        SELECT 
            f.id, f.name,
            COUNT(m.id) as member_count,
            AVG(m.age) as avg_age
        FROM family f
        LEFT JOIN family_member m ON f.id = m.family_id
        GROUP BY f.id, f.name
    """)
    List<FamilyStatisticsDTO> selectFamilyStatistics();
}
```

#### 8.2.3 复杂子查询

**问题**: DDD 框架不支持复杂的嵌套子查询

```java
// ❌ 不适用：需要子查询
// 查询成员数量大于平均值的家庭
SELECT f.*
FROM family f
WHERE f.person_count > (
    SELECT AVG(person_count) 
    FROM family
)

// ✅ 正确做法：使用 MyBatis 自定义 Mapper
@Mapper
public interface FamilyCustomMapper {
    @Select("""
        SELECT f.*
        FROM family f
        WHERE f.person_count > (
            SELECT AVG(person_count) FROM family
        )
    """)
    List<FamilyDO> selectFamiliesAboveAverage();
}
```

#### 8.2.4 跨聚合根的批量操作

**问题**: DDD 框架的批量操作仅限于单个聚合根内部

```java
// ❌ 不适用：需要跨聚合根批量更新
// 批量更新所有地址中的家庭名称
UPDATE family_address
SET family_name = '新名称'
WHERE family_id IN (
    SELECT id FROM family WHERE name = '旧名称'
)

// ✅ 正确做法：使用 MyBatis 自定义 Mapper
@Mapper
public interface FamilyAddressCustomMapper {
    @Update("""
        UPDATE family_address
        SET family_name = #{newName}
        WHERE family_id IN (
            SELECT id FROM family WHERE name = #{oldName}
        )
    """)
    int batchUpdateFamilyName(
        @Param("oldName") String oldName,
        @Param("newName") String newName
    );
}
```

#### 8.2.5 其他现有领域驱动接口无法支持的场景

包括但不限于：

- **窗口函数**：`ROW_NUMBER()`, `RANK()`, `LAG()`, `LEAD()` 等
- **递归查询**：CTE (Common Table Expressions) 递归查询树形结构
- **数据库特定函数**：MySQL 的 `JSON_EXTRACT`、PostgreSQL 的 `JSONB` 操作等
- **性能优化查询**：需要强制索引、查询提示等
- **复杂分页**：需要先分组再分页的复杂场景
- **数据迁移**：批量数据导入导出、ETL 操作

### 8.3 使用 MyBatis 自定义 SQL 的规范

当 DDD 框架无法满足需求时，遵循以下规范：

#### 8.3.1 文件组织

```
src/main/java/com/artframework/sample/
├── domains/
│   └── family/
│       ├── service/
│       │   └── FamilyService.java          # DDD 框架生成的
│       └── repository/
│           └── FamilyRepository.java       # DDD 框架生成的
│
├── mapper/                                  # 自定义 Mapper 存放位置
│   ├── FamilyCustomMapper.java             # 自定义查询 Mapper
│   └── FamilyMemberCustomMapper.java       # 自定义查询 Mapper
│
└── dto/                                     # 自定义 DTO
    ├── MemberAddressDTO.java
    └── FamilyStatisticsDTO.java
```

#### 8.3.2 Mapper 编写规范

```java
@Mapper
public interface FamilyCustomMapper {
    
    /**
     * 查询家庭成员及其住址信息
     * 
     * @param familyId 家庭ID
     * @param type 成员类型
     * @return 成员住址列表
     */
    @Select("""
        SELECT 
            m.id,
            m.name,
            m.phone,
            m.type,
            a.address_name
        FROM family_member m
        LEFT JOIN family_address a ON m.family_id = a.family_id
        WHERE m.family_id = #{familyId}
          AND m.type = #{type}
    """)
    List<MemberAddressDTO> selectMembersWithAddress(
        @Param("familyId") Long familyId,
        @Param("type") String type
    );
}
```

#### 8.3.3 DTO 定义规范

```java
@Data
@NoArgsConstructor
@AllArgsConstructor
@Builder
public class MemberAddressDTO {
    private Long id;
    private String name;
    private String phone;
    private String type;
    private String addressName;
}
```

#### 8.3.4 AppService 中调用自定义 Mapper

```java
@Service
@Slf4j
public class FamilyAppService {
    
    @Autowired
    private FamilyService familyService;
    
    @Autowired
    private FamilyCustomMapper familyCustomMapper;  // 自定义 Mapper
    
    /**
     * 查询家庭成员及其住址（使用自定义 SQL）
     */
    public List<MemberAddressDTO> getMembersWithAddress(Long familyId) {
        log.debug("查询家庭成员及住址: familyId={}", familyId);
        
        // 使用自定义 Mapper 执行 JOIN 查询
        List<MemberAddressDTO> result = familyCustomMapper
            .selectMembersWithAddress(familyId, "父亲");
        
        log.debug("查询结果: count={}", result.size());
        return result;
    }
    
    /**
     * 家庭统计（使用自定义 SQL）
     */
    public List<FamilyStatisticsDTO> getFamilyStatistics() {
        return familyCustomMapper.selectFamilyStatistics();
    }
}
```

### 8.4 决策流程图

```
需要实现某个数据查询功能
        ↓
是否涉及单个聚合根及其关联实体？
        ↓
    是 ↓                   ↓ 否
       ↓                   ↓
  使用 DDD 框架      是否需要 JOIN/统计/子查询？
       ↓                   ↓
   调用生成的          是 ↓                   ↓ 否
   DomainService        ↓                   ↓
                        ↓              使用 DDD 框架
                   使用 MyBatis        的 LambdaQuery
                   自定义 SQL
```

### 8.5 最佳实践总结

| 原则 | 说明 |
|------|------|
| **优先使用 DDD** | 单聚合根 CRUD 优先使用框架生成的方法 |
| **不要强行适配** | 复杂查询不要尝试用 DDD 框架勉强实现 |
| **自定义 SQL 独立** | 自定义 Mapper 与 DDD 框架代码分开存放 |
| **保持事务一致** | 自定义 SQL 操作也要纳入事务管理 |
| **文档化** | 在自定义 Mapper 方法上添加详细注释说明用途 |
| **测试覆盖** | 自定义 SQL 也要编写单元测试 |

---

## 📚 附录

### A. 框架核心类说明

| 类名 | 包路径 | 说明 |
|------|--------|------|
| `BaseDomain` | `io.github.roger3lee.domain.core.domain` | 基础领域对象基类 |
| `BaseAggregateDomain` | `io.github.roger3lee.domain.core.domain` | 聚合根基类 |
| `BaseRepository` | `io.github.roger3lee.domain.core.repository` | Repository 接口 |
| `BaseRepositoryImpl` | `io.github.roger3lee.domain.core.repository.impl` | Repository 实现基类 |
| `BaseDomainService` | `io.github.roger3lee.domain.core.service` | DomainService 接口 |
| `BaseDomainServiceImpl` | `io.github.roger3lee.domain.core.service.impl` | DomainService 实现基类 |
| `LambdaQuery` | `io.github.roger3lee.domain.core.lambda.query` | Lambda 查询构建器 |
| `PageDomain` | `io.github.roger3lee.domain.core.domain` | 分页参数 |

### B. 常用操作符（Op）速查表

| 操作符 | 说明 | SQL 等价 | 示例 |
|--------|------|---------|------|
| `EQ` | 等于 | `=` | `query.eq(FamilyDomain::getName, "张氏")` |
| `NE` | 不等于 | `!=` | `query.ne(FamilyDomain::getName, "测试")` |
| `GT` | 大于 | `>` | `query.gt(FamilyDomain::getPersonCount, 2)` |
| `GE` | 大于等于 | `>=` | `query.ge(FamilyDomain::getPersonCount, 2)` |
| `LT` | 小于 | `<` | `query.lt(FamilyDomain::getPersonCount, 10)` |
| `LE` | 小于等于 | `<=` | `query.le(FamilyDomain::getPersonCount, 10)` |
| `LIKE` | 模糊匹配 | `LIKE '%value%'` | `query.like(FamilyDomain::getName, "张")` |
| `LIKE_LEFT` | 左模糊 | `LIKE '%value'` | `query.likeLeft(FamilyDomain::getName, "家族")` |
| `LIKE_RIGHT` | 右模糊 | `LIKE 'value%'` | `query.likeRight(FamilyDomain::getName, "张氏")` |
| `IN` | 包含 | `IN (...)` | `query.in(FamilyDomain::getId, Arrays.asList(1L, 2L))` |
| `NOT_IN` | 不包含 | `NOT IN (...)` | `query.notIn(FamilyDomain::getId, Arrays.asList(1L))` |
| `ISNULL` | 为空 | `IS NULL` | `query.isNull(FamilyDomain::getHouseholder)` |
| `NOTNULL` | 不为空 | `IS NOT NULL` | `query.notNull(FamilyDomain::getName)` |

### C. 排序枚举（Order）

| 枚举值 | 说明 | 示例 |
|--------|------|------|
| `ASC` | 升序 | `query.orderBy(FamilyDomain::getCreateTime, Order.ASC)` |
| `DESC` | 降序 | `query.orderBy(FamilyDomain::getCreateTime, Order.DESC)` |

### D. 保存状态枚举（SaveState）

| 枚举值 | 说明 | 使用场景 |
|--------|------|---------|
| `INSERT` | 新增 | `afterSave(SaveState.INSERT)` |
| `UPDATE` | 更新 | `afterSave(SaveState.UPDATE)` |

---

## 🎯 总结

### 核心要点

1. **配置驱动开发**: 所有领域模型结构通过 `domain-{name}.xml` 定义
2. **自动生成代码**: 使用 `ddd-code-generator` 技能生成完整领域层代码
3. **禁止修改生成代码**: 生成的 Domain/Service/Repository 等代码**严禁手动修改**
4. **分层架构**: 严格遵守 `Controller → AppService → DomainService → Repository → Mapper` 调用链
5. **增量更新**: 框架自动对比新旧对象，只更新变化的字段
6. **级联操作**: 聚合根自动管理关联实体的插入、更新、删除
7. **类型安全查询**: 使用 Lambda 表达式构建查询条件，编译期类型检查

### 开发流程回顾

```
1. 编写 domain-{name}.xml（定义聚合根和关联关系）
    ↓
2. 准备 SQL DDL（定义表结构）
    ↓
3. 调用 ddd-code-generator 技能（生成领域层代码）
    ↓
4. 在 AppService 层编写业务逻辑（不修改生成的代码）
    ↓
5. 测试和部署
```

### 遵循本规范的优势

- ✅ **代码结构清晰**: 自动生成的代码遵循统一的架构模式
- ✅ **减少重复代码**: 框架提供完整的 CRUD 实现，无需手动编写
- ✅ **类型安全**: Lambda 查询提供编译期类型检查
- ✅ **高性能**: 增量更新、批量操作、懒加载等优化策略
- ✅ **易于维护**: 配置驱动，修改领域结构只需更新 XML 配置
- ✅ **保证一致性**: 自动生成的代码保证架构一致性

---

**文档版本**: v1.0  
**适用框架**: domain-core  
**配合技能**: ddd-code-generator  
**更新日期**: 2026-06-02
