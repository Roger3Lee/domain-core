---
paths:
  - "**/*.java"
---
# DDD 开发规范

> 配套工具：`domain-generator-v2-3.0.3.jar` — 从 XML + DDL 通过 CLI 命令行生成领域层代码。

## 开发工作流

1. 编写 `domain-{name}.xml`（定义聚合根及关联关系）
2. 准备 SQL DDL（表结构，支持 MySQL / PostgreSQL）
3. 运行 CLI 代码生成器（输入：XML + DDL → 输出：完整的领域层代码）
4. 将生成的代码复制到目标项目 `src/main/java` 中
5. 验证编译通过
6. 在 AppService 层编写业务逻辑 — **严禁修改生成的代码**

**代码生成器调用方式（CLI）**：

```bash
java -jar domain-generator-v2-3.0.3.jar \
    -d "path/to/domain-config.xml" \
    -s "path/to/schema.sql" \
    -o "path/to/output-dir" \
    -p "com.example.project" \
    --dialect postgresql
```

### CLI 参数说明

| 参数 | 必填 | 默认值 | 说明 |
|------|------|--------|------|
| `-d`, `--domain-xml` | 是 | — | 领域配置 XML 文件路径 |
| `-s`, `--sql-file` | 是 | — | SQL DDL 文件路径（MySQL 或 PostgreSQL） |
| `-o`, `--output-dir` | 是 | — | 生成代码的输出目录 |
| `-p`, `--base-package` | 是 | — | Java 基础包名（如 `com.example.project`） |
| `--dialect` | 否 | `mysql` | SQL 方言：`mysql` 或 `postgresql` |
| `--update-do` | 否 | `false` | 覆盖 DO 实体和 Mapper（当表字段变更时使用） |
| `--update-domain` | 否 | `false` | 覆盖领域层（Repository + Convertor，当领域模型变更时使用） |
| `--ignore-fields` | 否 | — | 要从 DO 中排除的列名，逗号分隔（如 `create_time,update_time,deleted`） |
| `--inherit` | 否 | — | 所有 DO 类的基类全限定名（如 `com.example.BaseEntity`） |
| `-h`, `--help` | 否 | — | 显示帮助信息 |
| `-V`, `--version` | 否 | — | 显示版本号 |

### 覆盖策略

生成器对不同文件类型采用不同的覆盖策略：

| 文件类型 | 覆盖行为 |
|----------|----------|
| Domain DTO、FindDomain、LambdaExp、Service、ServiceImpl | **始终覆盖**（每次生成都会重写） |
| DO 实体、Mapper | 仅在 `--update-do` 时覆盖 |
| Repository、RepositoryImpl、Convertor、ConvertorDecorator | 仅在 `--update-domain` 时覆盖 |
| AppService、AppServiceImpl、Controller | **仅首次生成**，之后永不覆盖（保护业务代码） |

> **提示**：生成器在内容相同时会跳过写入（文件标记为 "unchanged"），只有真正修改时才写入（"generated"）。

## 生成代码 — 严格只读

**禁止**手动修改以下任何生成的文件：
- `*Domain.java`、`*FindDomain.java`
- `*Service.java`、`*ServiceImpl.java`
- `*Repository.java`、`*RepositoryImpl.java`
- `*Mapper.java`、`*Convertor.java`、`*ConvertorDecorator.java`、`*LambdaExp.java`

**要修改领域结构** → 编辑 `domain-{name}.xml` → 重新运行代码生成器。
**要修改表结构字段** → 更新 DDL → 使用 `--update-do` 重新生成。
**要修改领域模型** → 编辑 XML → 使用 `--update-domain` 重新生成。
**要添加业务逻辑** → 在 AppService 层编写，调用生成的 DomainService 方法。

### 生成输出结构

```
{output-dir}/
└── {basePackagePath}/
    ├── entities/              {Name}DO.java（+ 关联表 DO）
    ├── mappers/               {Name}Mapper.java（+ 关联表 Mapper）
    ├── domains/
    │   └── {folder}/           # 按领域划分的子目录（默认与领域名小写同名）
    │       ├── domain/         {Name}Domain.java、{Name}FindDomain.java
    │       ├── repository/     {Name}Repository.java、{Related}Repository.java、impl/
    │       ├── service/        {Name}Service.java、impl/{Name}ServiceImpl.java
    │       ├── convertor/      {Name}Convertor.java、{Name}ConvertorDecorator.java
    │       └── lambdaexp/      {Name}LambdaExp.java
    ├── applications/          {Name}AppService.java、impl/{Name}AppServiceImpl.java（仅首次生成）
    └── controllers/           {Name}Controller.java（仅首次生成）
```

`{folder}` 由 XML 中 `<domain folder="...">` 属性指定；未设置时默认为领域名的驼峰转小写（如 `Family` → `family`、`OrderBilling` → `orderBilling`）。

## 分层架构

```
Controller → AppService → DomainService → Repository → Mapper → Database
```

| 层 | 职责 | 调用对象 | 禁止 |
|-------|---------------|-------|----------|
| Controller | 接收请求，校验参数，返回响应 | AppService | 直接调用 DomainService/Repository/Mapper |
| AppService | 业务编排，事务管理，跨领域协调 | DomainService | 直接调用 Repository/Mapper |
| DomainService | 领域对象 CRUD（**自动生成**） | Repository | 被手动修改 |
| Repository | 数据访问抽象（**自动生成**） | Mapper | 被手动修改 |
| Mapper | SQL 执行（**自动生成**） | Database | 被手动修改 |

### Controller 模板

```java
@RestController
@RequestMapping("/family/v1")   // 禁止添加 /api 前缀（由 context-path 处理）
public class FamilyController {
    @Autowired
    private FamilyAppService familyAppService;
}
```

### AppService 模板

```java
@Service
public class FamilyAppService {
    @Autowired
    private FamilyService familyService;

    @Transactional(rollbackFor = Exception.class)
    public Integer createFamily(FamilyDTO dto) {
        FamilyDomain family = FamilyConvertor.INSTANCE.toDomain(dto);
        return familyService.insert(family);
    }
}
```

## 聚合根模式

聚合根是领域模型的入口，管理关联实体的生命周期。

```java
public class FamilyDomain extends BaseAggregateDomain<FamilyDomain, FamilyService> {
    private Long id;
    private String name;
    private FamilyAddressDomain familyAddress;           // 一对一
    private List<FamilyMemberDomain> familyMemberList;   // 一对多
}
```

- 自动管理外键关系、级联增删改查、冗余字段同步

## Domain XML 配置

### 结构

```xml
<domains>
    <domain name="family"
            description="家庭领域模型"
            main-table="family"
            folder="family"
            implement="com.example.MyInterface">
        
        <!-- related：由聚合根管理，级联增删改查 -->
        <related name="members"
                 description="家庭成员"
                 table="family_member"
                 many="true"
                 fk="id:family_id"
                 redundancy="name:family_name"
                 deletable="true"
                 implement="com.example.MemberInterface">

            <!-- ref：独立生命周期，不级联 -->
            <ref name="address"
                 description="成员住址"
                 table="family_address"
                 many="false"
                 fk="id:family_member_id"
                 redundancy="name:address_name"/>
        </related>
    </domain>
</domains>
```

### `<domain>` 属性（聚合根定义）

| 属性 | 必填 | 描述 |
|------|------|------|
| `name` | 是 | 领域名（PascalCase，如 `Family` 生成 `FamilyDomain`） |
| `main-table` | 是 | 主数据库表名 |
| `description` | 否 | API 文档描述 |
| `folder` | 否 | 自定义子包路径（如 `order/billing` → 包 `order.billing`），默认取领域名驼峰转小写 |
| `implement` | 否 | Domain 类实现的接口全限定名 |

### `<aggregate>` 元素（可选，聚合根表配置）

与 `<related>` 共享相同的属性结构（参见下方属性表），用于描述聚合根本身对应表的映射细节。通常用于指定聚合根表的外键与 ID 的对应关系。

### `<related>` 属性（关联实体，级联管理）

| 属性 | 必填 | 默认值 | 描述 |
|------|------|--------|------|
| `table` | 是 | — | 关联表名（必须与 DDL 一致） |
| `many` | 否 | `false` | `true`=一对多，`false`=一对一 |
| `fk` | 否 | — | `主表列:外键列`，多字段用 `|` 分隔 |
| `redundancy` | 否 | — | `主表列:冗余列`，多字段用 `|` 分隔 |
| `name` | 否 | 表名 | 自定义关联实体名称 |
| `description` | 否 | — | 描述说明 |
| `implement` | 否 | — | 关联 Domain 类实现的接口全限定名 |
| `deletable` | 否 | `true` | 是否参与级联删除 |

### `<ref>` 属性（引用关系，独立生命周期）

| 属性 | 必填 | 默认值 | 描述 |
|------|------|--------|------|
| `table` | 是 | — | 引用表名（必须与 DDL 一致） |
| `name` | 否 | 表名 | 引用字段名（驼峰命名） |
| `many` | 否 | `false` | `true`=一对多，`false`=一对一 |
| `fk` | 否 | — | `主表列:外键列`，多字段用 `|` 分隔 |
| `redundancy` | 否 | — | `主表列:冗余列`，多字段用 `|` 分隔 |
| `description` | 否 | — | 描述说明 |

### `<related>` 与 `<ref>` 对比

| 特性 | `<related>` | `<ref>` |
|---------|------------|---------|
| 生命周期 | 由聚合根管理 | 独立 |
| 级联增删改查 | 自动 | 否 |
| 数据加载 | 独立查询 | 从父级 related 列表中过滤 |
| 存储方式 | 直接字段 | `CacheDomain` 包装器 |
| 使用场景 | 聚合内的实体 | 对外部实体的引用 |

### 外键语法

```xml
fk="id:family_id"                              <!-- 单字段 -->
fk="id:family_id|type:member_type"             <!-- 多字段 -->
fk="'ACTIVE'(String):status(VARCHAR)"          <!-- 常量值 -->
redundancy="name:family_name"                  <!-- 冗余字段 -->
redundancy="name:family_name|code:family_code" <!-- 多字段冗余 -->
```

## 框架 API 参考

### 加载

```java
FamilyDomain family = FamilyDomain.load(1L, familyService);
family.loadRelated(FamilyDomain.FamilyAddressDomain.class);
family.loadRelated(FamilyDomain.FamilyMemberDomain.class, query -> {
    query.eq(FamilyMemberDomain::getType, "父亲")
         .orderBy(FamilyMemberDomain::getCreateTime, Order.DESC);
});
```

### Lambda 查询

```java
LambdaQuery<FamilyDomain> query = LambdaQuery.of(FamilyDomain.class);
query.eq(FamilyDomain::getName, "张氏家族")
     .gt(FamilyDomain::getPersonCount, 2)
     .orderBy(FamilyDomain::getCreateTime, Order.DESC);

familyService.queryOne(FamilyDomain.class, query);
familyService.queryList(FamilyDomain.class, query);
familyService.queryPage(FamilyDomain.class, pageDomain, query);
familyService.queryCount(FamilyDomain.class, query);
```

**运算符**：`eq` `ne` `gt` `ge` `lt` `le` `like` `likeLeft` `likeRight` `in` `notIn` `between` `isNull` `notNull` `and()` `or()`

**自定义 SQL 条件**（如 PostgreSQL ILIKE）：

```java
query.condition(fieldName, "ILIKE {0}", value);
```

### LoadFlag 查询控制

```java
FamilyFindDomain findDomain = FamilyFindDomain.builder()
    .key(1L)
    .loadFlag(new FamilyDomain.LoadFlag() {{
        setLoadFamilyMemberDomain(true);
        setLoadFamilyAddressDomain(false);
    }})
    .build();
FamilyDomain family = familyService.find(findDomain);
```

LoadFlag 查询属性：`filter`（条件组）、`order`（排序）、`select`（字段白名单）、`exclude`（字段黑名单）。

### 新增（级联）

```java
FamilyDomain family = new FamilyDomain();
family.setName("张氏家族");
family.setFamilyAddress(address);
family.setFamilyMemberList(members);
Integer id = familyService.insert(family);  // 所有 ID 自动回填
```

### 更新（增量）

```java
FamilyDomain oldFamily = FamilyDomain.load(1L, familyService);
oldFamily.loadRelated(FamilyDomain.FamilyMemberDomain.class);

FamilyDomain newFamily = new FamilyDomain();
newFamily.setId(1L);
newFamily.setName("新名称");
familyService.update(newFamily, oldFamily);  // 仅更新了 name

// 一对多：设置新的列表 → 框架执行 MERGE（删除旧的 + 插入新的）
oldFamily.setFamilyMemberList(newMembers);
familyService.update(oldFamily, oldFamily);
```

### 删除（级联）

```java
familyService.delete(1L);  // 级联删除所有关联实体
```

### 序列化

内部字段（`_service`、`_thisDomain` 等）会被排除在序列化之外。领域对象可以直接返回给前端。

## 方言差异

| 特性 | MySQL | PostgreSQL |
|------|-------|------------|
| 主键策略 | `IdType.AUTO`，无需 `@KeySequence` | `IdType.INPUT`，`@KeySequence("seq_{table}_id")` |
| 列名处理 | 保留 DDL 中的原始形式 | 未加引号的标识符转为小写 |
| 表注释 | `COMMENT = '...'` 在 CREATE TABLE 之后 | `COMMENT ON TABLE ... IS '...'` |
| 列注释 | `COMMENT '...'` 内联 | `COMMENT ON COLUMN ... IS '...'` |
| 语法差异 | 反引号包裹标识符 | 不加引号的标识符 |

## DDD 框架与自定义 SQL 的选择

**使用 DDD 框架（生成的 DomainService）的场景：**
- 单个聚合根的增删改查
- 级联操作
- 增量更新
- Lambda 查询（类型安全，编译时检查）
- 通过 `loadRelated` 实现懒加载

**使用 MyBatis 自定义 SQL 的场景：**
- 多表 JOIN（跨聚合根）
- GROUP BY / 聚合函数
- 复杂子查询
- 跨聚合根批量操作
- 窗口函数、CTE 递归查询

### 决策流程

> ⚠️ **注意优先级顺序**：LambdaQuery 是 MyBatis 自定义 SQL 之前的必经步骤，不是备用选项。

```
第一步：DomainService 能否处理？ → 能 → 使用 DomainService ✅  结束
第二步：LambdaQuery 能否处理？   → 能 → 使用 LambdaQuery ✅    结束
第三步：写注释说明为什么前两步都不行 → 才可使用 MyBatis 自定义 SQL ⚠️
```

**LambdaQuery 覆盖的场景（无需自定义 SQL）**：
- 任意字段的条件过滤（eq/ne/gt/ge/lt/le/between/isNull）
- 模糊查询（like/likeLeft/likeRight）
- 多值查询（in/notIn）
- AND/OR 逻辑组合
- 排序、分页
- 批量按 ID 加载（in + queryList）
- 自定义 SQL 条件运算符（如 PostgreSQL `ILIKE`）

**自定义 SQL 仅在以下情况成立（必须在代码注释中说明原因）**：
- GROUP BY / 聚合函数（COUNT、SUM、AVG 等）
- 跨聚合根的 JOIN 且无法拆分为两次独立查询后内存合并
- 窗口函数、CTE 递归查询
- 跨聚合根的批量原子操作

### 自定义 Mapper 规则

- 必须放在与生成代码不同的目录：`mapper/{Name}CustomMapper.java`
- 必须从 AppService 层调用
- 必须纳入 `@Transactional` 事务范围

```java
@Mapper
public interface FamilyCustomMapper {
    @Select("""
        SELECT m.id, m.name, a.address_name
        FROM family_member m
        LEFT JOIN family_address a ON m.family_id = a.family_id
        WHERE m.type = #{type}
    """)
    List<MemberAddressDTO> selectMembersWithAddress(@Param("type") String type);
}
```

## 最佳实践

- **离线生成**：代码生成器解析 SQL DDL 文件而非连接数据库，确保生成结果的确定性和 CI/CD 可重复性
- **优先使用 DDD**：单聚合根增删改查使用生成的 DomainService 方法
- **不要强行套用**：复杂查询使用 MyBatis 自定义 SQL
- **增量更新策略**：表结构变更使用 `--update-do`，领域模型变更使用 `--update-domain`，业务代码（AppService/Controller）永久保护
- **避免 N+1**：分页查询后批量加载关联数据 — 切勿在分页记录的循环中调用 `loadRelated`
- **分页上限**：框架默认最多 10,000 条记录
- **事务一致性**：自定义 SQL 也必须放在 `@Transactional` 中
- **基类抽取**：如果所有表共享审计字段（如 `create_time`、`update_time`、`deleted`），使用 `--ignore-fields` 排除这些列，并用 `--inherit` 指向包含这些字段的基类

## 核心类参考

| 类 | 包路径 |
|-------|---------|
| `BaseDomain` | `io.github.roger3lee.domain.core.domain` |
| `BaseAggregateDomain` | `io.github.roger3lee.domain.core.domain` |
| `BaseLoadFlag` | `io.github.roger3lee.domain.core.domain` |
| `CacheDomain` | `io.github.roger3lee.domain.core.domain` |
| `PageDomain` | `io.github.roger3lee.domain.core.domain` |
| `BaseRepository` | `io.github.roger3lee.domain.core.repository` |
| `BaseDomainService` | `io.github.roger3lee.domain.core.service` |
| `BaseSimpleDomainService` | `io.github.roger3lee.domain.core.service` |
| `LambdaQuery` | `io.github.roger3lee.domain.core.lambda.query` |
| `LambdaOrder` | `io.github.roger3lee.domain.core.lambda.order` |
| `LambdaCache` | `io.github.roger3lee.domain.core.lambda` |
| `Order.ASC` / `Order.DESC` | 排序枚举 |
| `Op` | 比较运算符枚举（EQ、IN、LIKE 等） |
| `SaveState` | 保存状态枚举（INSERT、UPDATE） |
| `BatchBaseMapper` | `io.github.roger3lee.domain.core.mapper` |

## SQL-to-Java 类型映射

| SQL 类型 | Java 类型 |
|----------|-----------|
| BIGINT、BIGSERIAL、SERIAL(8) | `Long` |
| INT、INTEGER、SERIAL(4) | `Integer` |
| NUMERIC(n)、DECIMAL(n)（n≤9） | `Integer` |
| NUMERIC(n,m)、DECIMAL(n,m) | `java.math.BigDecimal` |
| VARCHAR、CHAR、TEXT | `String` |
| BOOLEAN、TINYINT | `Boolean` |
| DATE | `java.time.LocalDate` |
| TIMESTAMP、DATETIME | `java.time.LocalDateTime` |
| TIME | `java.time.LocalTime` |
